package liveClass;

public class BufferedReader {
//  public static void main(String[] args) { Path path = Paths .get("/Users/susmitsarkar/spring-boot/spring-swagger/src/main/java/com/upgrad/spring/swagger/api/config/ParentClass.java"); boolean exists = Files.exists(path); System.out.println("Exists = " + exists); try (BufferedReader reader = Files.newBufferedReader(path, StandardCharsets.ISO_8859_1);) { String line = reader.readLine(); while (line != null) { System.out.println("Line = " + line); line = reader.readLine(); } } catch (IOException ioe) { ioe.printStackTrace(); } }
}
